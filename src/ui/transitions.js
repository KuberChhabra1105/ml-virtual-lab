import gsap from 'gsap';

export default class Transitions {
    constructor() {
        // Setup transition overlays if needed
    }

    static splitReveal(element) {
        // Helper for text reveal
        // Implementation depend on splitting library or manual logic
    }
}
